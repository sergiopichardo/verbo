{
  "name": "verbo",
  "private": true,
  "scripts": {
    "fe:dev": "npm run dev --workspace=@verbo/frontend",
    "be:deploy": "npm run deploy --workspace=@verbo/backend",
    "be:destroy": "npm run destroy --workspace=@verbo/backend",
    "utils-layer:build": "npm run build --workspace=@verbo/utils"
  },
  "workspaces": [
    "apps/*",
    "packages/**"
  ],
  "dependencies": {
    "http-status-codes": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.6.1"
  }
}
